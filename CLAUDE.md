# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository state

This repository currently contains a single file, `index.html`, which is empty (0 bytes). There is no build system, package manager, framework, test suite, linter, or CI configuration set up yet — there is nothing to build, lint, or test.

Because the project has no established structure or conventions yet, do not assume a framework, language ecosystem, or directory layout. When the user asks you to start building the site, ask (or infer from their request) what kind of site/stack they want (e.g., static HTML/CSS/JS, or a specific framework) before scaffolding, and update this file with real commands and architecture notes once that structure exists.
